# API key pública de Firebase (Project Settings → General → Tus apps → Web app)
VITE_FB_API_KEY=tu-api-key-de-firebase

# URL de tu backend (jobtrack-ai). En local suele ser http://localhost:3000
VITE_API_URL=http://localhost:3000
